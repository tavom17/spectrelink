import BigNumber from 'bignumber.js';
import type { TokenConfig, Tx } from '@irys/upload-core';
import type { Finality } from '@solana/web3.js';
import { Transaction } from '@solana/web3.js';
import SolanaConfig from './token';
export type GetFeeResult = {
    computeBudget: BigNumber;
    computeUnitPrice: BigNumber;
};
export type SPLTokenConfig = TokenConfig<any, {
    finality?: Finality;
    disablePriorityFees?: boolean;
}> & {
    contractAddress: string;
};
export declare class SPLConfig extends SolanaConfig {
    protected contractAddress: string;
    protected decimals: number;
    constructor(config: SPLTokenConfig);
    getSPLMetadata(): Promise<void>;
    getTx(txId: string): Promise<Tx>;
    _createTxUnsigned(amount: BigNumber.Value, to: string, fee?: GetFeeResult): Promise<Transaction>;
    ready(): Promise<void>;
}
export default SPLConfig;
